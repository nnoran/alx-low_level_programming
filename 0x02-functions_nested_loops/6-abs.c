#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *@int: number whose abs is computed
 * Return: 0 Always (success)
 */
int _abs(int)
{
	int n, a;

	a = n * -1

	if (n >= 0)
	{
		_putchar('n');
	}
	else if (n < 0)
	{
		_putchar('a');
	}
	return (0);
}

