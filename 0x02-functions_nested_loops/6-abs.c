#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *@n: number whose abs is computed
 * Return: 0 Always (success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		_putchar('n');
	}
	else if (n < 0)
	{
		_putchar('(n * (-1))');
	}
	return (0);
}

