#include "main.h"

/**
 * _abs - computes absolute value of an integer
 *@n: number whose abs is computed
 * Return: theabs of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
}

