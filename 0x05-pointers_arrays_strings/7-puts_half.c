#include "main.h"

/**
 * putd_half - prints half of a string
 * @str: char to check
 *
 */
void puts_half(char *s)
{
	int string = 0;

	while (str[string] != '\0')
		n = (string - 1) / 2;
	else
		n = (string / 2);
	n++;
	for (string = n; str[string] != '\0'; string++)
		_putchar(str[string]);
	_putchar('\n');
}
